# Instrucciones para ejecutar el proyecto

## Instalación
1. Abrir terminal en el directorio del proyecto
2. Ejecutar: `npm install`

## Ejecución
1. Iniciar el servidor: `npm start`
2. Abrir navegador en: http://localhost:3001
3. Los archivos de usuarios se guardarán automáticamente en el directorio del proyecto

## Desarrollo
- Para desarrollo con auto-reload: `npm run dev`

## Archivos generados
- Los usuarios se guardan en archivos JSON con formato: `usuarios_YYYY-MM-DD.json`
- Los archivos se crean en el mismo directorio que index.html